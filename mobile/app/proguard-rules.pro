# Reglas ProGuard/R8 del scaffold. Se amplían a medida que se agreguen features
# que las requieran (p.ej. kotlinx.serialization necesita mantener los
# @Serializable si en el futuro se activa minify en debug).
